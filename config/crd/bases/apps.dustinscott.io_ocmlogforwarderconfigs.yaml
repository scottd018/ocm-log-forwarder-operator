---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: ocmlogforwarderconfigs.apps.dustinscott.io
spec:
  group: apps.dustinscott.io
  names:
    kind: OCMLogForwarderConfig
    listKind: OCMLogForwarderConfigList
    plural: ocmlogforwarderconfigs
    singular: ocmlogforwarderconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OCMLogForwarderConfig is the Schema for the ocmlogforwarderconfigs
          API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: OCMLogForwarderConfigSpec defines the desired state of OCMLogForwarderConfig.
            properties:
              backend:
                properties:
                  elasticSearch:
                    properties:
                      authType:
                        default: basic
                        description: '(Default: "basic") ElasticSearch authentication
                          type to use.  Only ''basic'' supported at this time.  Requires
                          a single key/value pair stored in a secret named ''elastic-auth''
                          which contains the basic authentication info for the ElasticSearch
                          connection.  Secret must exist within the same namespace
                          where the OCM Log Collector is deployed to.'
                        enum:
                        - basic
                        type: string
                      index:
                        default: ocm_service_logs
                        description: '(Default: "ocm_service_logs") Index name in
                          ElasticSearch where service logs are sent.  Index name must
                          be 128 characters or less.'
                        maxLength: 128
                        type: string
                      url:
                        default: https://elasticsearch-es-http.elastic-system.svc.cluster.local:9200
                        description: '(Default: "https://elasticsearch-es-http.elastic-system.svc.cluster.local:9200")
                          URL to which to ship logs when using the ''elasticsearch''
                          as a backend in the .spec.backend.type field of this custom
                          resource.'
                        type: string
                    type: object
                  type:
                    default: elasticsearch
                    description: '(Default: "elasticsearch") Backend type where logs
                      are sent and stored.  Only ''elasticsearch'' supported at this
                      time.  Requires backend.elasticSearch.url to be set.'
                    enum:
                    - elasticsearch
                    type: string
                type: object
              forwarderNamespace:
                default: ocm-log-forwarder
                description: '(Default: "ocm-log-forwarder") Namespace used to provision
                  the OCM Log Forwarder workloads.  Each workload provisoned will
                  end up in this namespace.'
                type: string
              provider:
                default: rosa
                description: '(Default: "rosa") Provider to which the OCM Log Forwarder
                  is provisioned to.  Only ''rosa'' supported at this time.'
                enum:
                - rosa
                type: string
            type: object
          status:
            description: OCMLogForwarderConfigStatus defines the observed state of
              OCMLogForwarderConfig.
            properties:
              conditions:
                items:
                  description: PhaseCondition describes an event that has occurred
                    during a phase of the controller reconciliation loop.
                  properties:
                    lastModified:
                      description: LastModified defines the time in which this component
                        was updated.
                      type: string
                    message:
                      description: Message defines a helpful message from the phase.
                      type: string
                    phase:
                      description: Phase defines the phase in which the condition
                        was set.
                      type: string
                    state:
                      description: PhaseState defines the current state of the phase.
                      enum:
                      - Complete
                      - Reconciling
                      - Failed
                      - Pending
                      type: string
                  required:
                  - lastModified
                  - message
                  - phase
                  - state
                  type: object
                type: array
              created:
                type: boolean
              dependenciesSatisfied:
                type: boolean
              resources:
                items:
                  description: ChildResource is the resource and its condition as
                    stored on the workload custom resource's status field.
                  properties:
                    condition:
                      description: ResourceCondition defines the current condition
                        of this resource.
                      properties:
                        created:
                          description: Created defines whether this object has been
                            successfully created or not.
                          type: boolean
                        lastModified:
                          description: LastModified defines the time in which this
                            resource was updated.
                          type: string
                        message:
                          description: Message defines a helpful message from the
                            resource phase.
                          type: string
                      required:
                      - created
                      type: object
                    group:
                      description: Group defines the API Group of the resource.
                      type: string
                    kind:
                      description: Kind defines the kind of the resource.
                      type: string
                    name:
                      description: Name defines the name of the resource from the
                        metadata.name field.
                      type: string
                    namespace:
                      description: Namespace defines the namespace in which this resource
                        exists in.
                      type: string
                    version:
                      description: Version defines the API Version of the resource.
                      type: string
                  required:
                  - group
                  - kind
                  - name
                  - namespace
                  - version
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
